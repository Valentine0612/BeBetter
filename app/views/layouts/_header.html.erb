<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <a class="navbar-brand" href="<%= root_path %>">A-Сервис</a>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="<%= tasks_new_path %>">Новая задача<span class="sr-only"></span></a>
        </li> 
        <li class="nav-item active">
          <a class="nav-link" href="<%= tasks_path %>">Просмотр задач<span class="sr-only"></span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="<%= current_user %>">Профиль<span class="sr-only"></span></a>
        </li>

      </ul>
      <% if logged_in? %>
        <%= link_to(t(:'Выйти'), logout_path, class: "btn btn-light", method: :delete) %>
      <% else %>
        <%= link_to(t(:'Войти'), login_path, class: "btn btn-light") %>
        <%= link_to(t(:'Регистрация'), signup_path, class: "btn btn-outline-light") %>
      <% end %>
    </div>
</nav>