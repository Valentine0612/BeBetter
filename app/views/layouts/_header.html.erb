<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <a class="navbar-brand" href="<%= root_path %>">BeBetter</a>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav mr-auto">
      <% if logged_in? %>
        <li class="nav-item active">
          <a class="nav-link" href="<%= tasks_new_path %>">Новая задача<span class="sr-only"></span></a>
        </li> 
        <li class="nav-item active">
          <a class="nav-link" href="<%= user_path(current_user) %>">Просмотр задач<span class="sr-only"></span></a>
        </li>
          <li class="nav-item active">
          <a class="nav-link" href="<%= edit_user_path(current_user) %>">Профиль<span class="sr-only"></span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="<%= users_path %>">Пользователи<span class="sr-only"></span></a>
        </li>
        <%end%>

      </ul>
      <% if logged_in? %>
        <%= link_to(t(:'Выйти'), logout_path, class: "btn btn-light", method: :delete) %>
      <% else %>
        <%= link_to(t(:'Войти'), login_path, class: "btn btn-light") %>
        <%= link_to(t(:'Регистрация'), signup_path, class: "btn btn-outline-light") %>
      <% end %>
    </div>
</nav>